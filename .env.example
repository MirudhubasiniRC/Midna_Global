# Shared defaults (optional). Mode-specific files override these.
# Copy to .env.local for machine-only overrides (gitignored via *.local).

VITE_APP_ENV=development
VITE_API_BASE_URL=http://localhost:5001
VITE_API_TIMEOUT_MS=15000
